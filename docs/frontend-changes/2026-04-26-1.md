# 🟢 NON-BREAKING Kullanıcı wellness API’leri ve profil genişletmesi

- **ID**: 2026-04-26-1
- **Tarih**: 2026-04-26
- **Etkilenen**: Yeni AppService uçları; `UserProfileDto` (hesaplanan alanlar)
- **Değişiklik Tipi**: Yeni uçlar + yanıta alan ekleme (geriye uyumlu)
- **Açıklama**:
  - Kullanıcı kendi **bildirim tercihlerini** yönetir (`IUserNotificationPreferencesAppService`).
  - **Kişisel antrenman programı** (şablon hareketler) ve **antrenman günlüğü** (MET×kg×süre ile kcal) (`IPersonalWorkoutProgramAppService`, `IUserWorkoutLogAppService`).
  - **Beslenme planı**, **gıda kataloğu** (100g başına değerler), **günlük öğün kayıtları** ve **gün sonu özet** (`IPersonalNutritionPlanAppService`, `IUserFoodItemAppService`, `IUserMealLogAppService`, `IWellnessDailySummaryAppService`).
  - `GetMyProfile` yanıtında: `BmiCategory`, `BmiCategoryDescription`, `IsObese` (VKİ sınıfı — bilgilendirme amaçlı).
- **İstemcinin Yapması Gerekenler**:
  - Yeni ekranlar: wellness/beslenme/antrenman yönetimi; swagger’dan veya ABP dynamic API yollarından uç imzalarını alın.
  - Profil: VKİ sınıfı alanlarını (isteğe bağlı) gösterebilirsiniz; mevcut tüketicileri kırmak için yeni alanlar opsiyonel.
- **Detaylar**: [CHANGELOG girişi](../CHANGELOG.md), migration: `AddWellnessUserProgramsAndLogs`.
